<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title><%= typeof title !== 'undefined' ? title : 'AGUDASCO' %></title>
  <link rel="stylesheet" href="/public/css/styles.css"/>
</head>
<body>
  <header class="header">
    <div class="container">
      <div class="brand">
        <img src="https://res.cloudinary.com/<%= (process.env.CLOUDINARY_CLOUD_NAME||'dsyo0e3fu') %>/image/upload/c_scale,w_40/v1710000000/logo.png" alt="Logo" width="40" height="40" />
        <div class="title">AGUDASCO</div>
      </div>
      <nav class="nav">
        <a href="/" class="<%= (active==='home')?'active':'' %>">Beranda</a>
        <a href="/artikel" class="<%= (active==='artikel')?'active':'' %>">Artikel</a>
        <a href="/laporan" class="<%= (active==='laporan')?'active':'' %>">Laporan</a>
        <a href="/adart" class="<%= (active==='adart')?'active':'' %>">AD/ART</a>
        <a href="/anggota" class="<%= (active==='anggota')?'active':'' %>">Anggota</a>
        <a href="/galeri" class="<%= (active==='galeri')?'active':'' %>">Galeri</a>
        <a href="/tentang" class="<%= (active==='tentang')?'active':'' %>">Tentang</a>
        <a href="/kontak" class="<%= (active==='kontak')?'active':'' %>">Kontak</a>
      </nav>
    </div>
  </header>

  <main>
    <div class="container">
      <%- body %>
    </div>
  </main>

  <footer class="footer">
    <div class="container">
      <div class="grid">
        <div>
          <div class="brand">
            <div class="title">AGUDASCO</div>
          </div>
          <p class="meta">Organisasi alumni – membangun jaringan dan kontribusi nyata.</p>
        </div>
        <div>
          <h4>Kontak</h4>
          <p class="meta">Email: info@agudasco.org<br/>Alamat: Jakarta</p>
        </div>
        <div>
          <h4>Tautan</h4>
          <p><a href="/laporan">Laporan Keuangan</a><br/><a href="/adart">AD/ART</a></p>
        </div>
      </div>
      <p class="meta" style="margin-top:20px;">© <%= new Date().getFullYear() %> AGUDASCO</p>
    </div>
  </footer>

  <!-- Lightbox -->
  <div id="lightbox" class="lightbox-backdrop" onclick="closeLightbox(event)">
    <div>
      <img id="lightbox-img" class="lightbox-img" src="" alt=""/>
      <div id="lightbox-cap" class="lightbox-caption"></div>
    </div>
  </div>
  <script>
    function openLightbox(src, cap){ 
      const b=document.getElementById('lightbox'); 
      document.getElementById('lightbox-img').src=src; 
      document.getElementById('lightbox-cap').textContent=cap||''; 
      b.classList.add('active');
    }
    function closeLightbox(e){
      if(e.target.id==='lightbox' || e.target.id==='lightbox-img'){
        document.getElementById('lightbox').classList.remove('active');
      } else {
        document.getElementById('lightbox').classList.remove('active');
      }
    }
  </script>
</body>
</html>
