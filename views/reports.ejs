<h1>Laporan Keuangan</h1>
<ul>
  <% reports.forEach(r => { %>
    <li>
      <a href="<%= r.file %>" target="_blank"><%= r.title %></a>
      (<%= new Date(r.created_at).toLocaleDateString() %>)
    </li>
  <% }) %>
</ul>
