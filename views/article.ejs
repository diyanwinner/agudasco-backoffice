<h1><%= article.title %></h1>
<div class="muted"><%= new Date(article.created_at).toLocaleString() %></div>
<% if (article.image) { %>
  <img src="<%= article.image %>" alt="<%= article.title %>" style="max-width:100%; border-radius:8px;">
<% } %>
<p><%- article.content %></p>
